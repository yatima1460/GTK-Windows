# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

jobs:
- job:
  displayName: "GTK x86_64"
  pool:
    name: 'Hosted VS2017'


  steps:
  - script: git clone --single-branch --branch 2019.07 https://github.com/Microsoft/vcpkg.git
  - script: cd vcpkg
  - script: bootstrap-vcpkg.bat
  - script: vcpkg integrate install
  - script: vcpkg install gtk:x64-windows
  - script: vcpkg export --zip gtk:x64-windows

- job:
  displayName: "GTK x86"
  pool:
    name: 'Hosted VS2017'

  steps:
  - script: git clone --single-branch --branch 2019.07 https://github.com/Microsoft/vcpkg.git
  - script: cd vcpkg
  - script: bootstrap-vcpkg.bat
  - script: vcpkg integrate install
  - script: vcpkg install gtk:x86-windows
  - script: vcpkg export --zip gtk:x86-windows
  

